# Rime 用戶自定義配置

patch:
  schema_list:
    - schema: double_pinyin_flypy # 小鶴

  switcher:
    hotkeys:
      - F4

  ascii_composer:
    good_old_caps_lock: true
    # 将几个按键无效化
    switch_key:
      Caps_Lock: noop
      Shift_L: noop
      Shift_R: noop
